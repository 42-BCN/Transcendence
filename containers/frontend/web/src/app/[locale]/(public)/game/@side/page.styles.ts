export const gameSidePageStyles = {
  // Ensure the game chat toggle sits above nav overlays and other UI layers
  toggleButton: 'absolute right-4 top-16 z-[200] pointer-events-auto md:top-4',
  // Place the chat container above game phases/steps so messages are visible
  chatWrapper: 'ml-auto h-full w-full max-w-sm md:max-w-none relative z-[200]',
};
